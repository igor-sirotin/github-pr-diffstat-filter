{
  "name": "gh-pr-diffstat-filter",
  "version": "0.1.0",
  "description": "Browser extension: GitHub PR diffstat without lockfiles",
  "scripts": {
    "test": "node --test test/*.test.js",
    "e2e": "node scripts/e2e.mjs",
    "package": "rm -f gh-pr-diffstat-filter.zip && zip -r gh-pr-diffstat-filter.zip manifest.json src"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  },
  "private": true,
  "type": "commonjs"
}
